   <div class= "details">
<p>Notes Entered By <br>
<h5><u><%="#{(@user.employeeInit).upcase}"%></u></h5></p>
<hr>
<% @interaction.each do |interaction| %>
   <ol>
    <li><%= interaction.created_at.strftime("%A, %B %d %Y at %I:%M %P") %></li>
    <li><%= interaction.content %></li>

    </ol>
    <hr>
<%end%>
    <%= link_to "back", cards_path %>
</div>